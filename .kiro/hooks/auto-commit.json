{
  "name": "Auto-commit tras cada respuesta",
  "version": "1.0.0",
  "description": "Hace commit y push automático de todos los cambios después de que el agente termina de responder",
  "when": {
    "type": "agentStop"
  },
  "then": {
    "type": "runCommand",
    "command": "cd c:\\Users\\adria\\JuegoTalentLand\\cmaenad.github.io && git add -A && git diff --cached --quiet || git commit -m \"chore: auto-commit agente\" && git push"
  }
}
